import { all, takeEvery, put, call } from 'redux-saga/effects';
import FilmsType from './types';
// import httpUser from '../../api/films'


export default function* filmsSaga() {
  yield all([
  ]);
}
